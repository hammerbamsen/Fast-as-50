# Ændringer 6/9-2026 — blok 6: Krop-fanen svarer på "går cuttet og vanerne den rigtige vej?"

Branch `claude/0906-krop-fase1` (oven på origin/main 6/9). Tre commits: pipeline, index.html, ændringslog. Ingen push.
Tests: `python3 -m pytest scripts/modules -q` → **538 passed, 1 skipped** (før: 511; +27 i `test_body.py`).

## A. Pipeline

| Fil | Hvad |
|---|---|
| `scripts/modules/body.py` (ny) | `build_body(plan, data, today, strength_log=None)` → `data.body` = `{asOf, glidepath, fat, ffm, strengthWeek, strengthWeeks, cutCheck}`. Ren beregning, ingen netværk. `find_weight_plan` tager det aktive programs `weightPlan`, ellers det første kommende program med et (6/9 er medoc-2026 aktivt uden weightPlan; cuttet ligger i tds-2027). **glidepath**: `phase` pre/cut/hold (hold fra `holdFromMonth` eller efter `targetDate`), `cutWeek`/`cutWeeks` (19), `isoWeek`, `cutStartIsoWeek` (39), `expectedKg` lineær 72,2→68, `ratePerWeek` −0,22 (negativt = tab), `actualRate4w`, `avg7`, `delta`, `corridorKg` (0,5; hold 1,0), `status` foran/plan/bagud, `note`, `mondays`, `series` (hver mandag fra 21/9 + 31/1). Forventningen til et 7d-snit tages **midt i vinduet** (`expected_for_avg`, dag −3) — ellers ligger et trailing snit altid ~0,1 kg "bagud" på en faldende linje. 2-mandags-reglen: uden for korridoren i dag giver kun foran/bagud hvis de to seneste mandage (inkl. i dag) lå samme vej; ellers 'plan' med note. **fat**: `avg14`, `avg14Change28d`, `expected` (21,8→16, samme vindue-centrering med 7 dage), `status` ±0,5 pp. **ffm** = avg14(vægt)×(1−avg14(fedt)/100): `now`, `change28d`, `target` 57,1, status ok/warn (< −0,5). **cutCheck** (kun `phase == 'cut'`): signaler `rate` (7d-snit Δ over 14 d /2: > 0,4 warn, > 0,6 act), `ffm` (< −0,5/28 d act), `strength` (< mål i de to seneste afsluttede uger → warn), `recovery` (RHR +2 og HRV −5 % over 14 d mens vægt falder → warn), `plateau` (|Δ7d| < 0,2 over 3 uger → info). `level` = højeste, `text` = ÉN handling (prioritet ffm-act > rate-act > rate-warn > recovery > strength > plateau > "På plan — fortsæt"); "styrke torsdag" fra næste ikke-gennemførte styrkepas i `week_sessions`. Manglende serier → `'ingen data'` pr. signal, tjekket forsvinder aldrig. I pre: `text` "Aktiveres uge 39 (21/9)". `weight_kpi`/`fat_kpi` → `{value (7d/14d-snit), unit, sub, color}`; `cut_warning` → `{type:'cut', level warn/critical}` eller None. |
| `scripts/update_kpi.py` | Efter vægt-sub: henter 28 dages aktiviteter (ét kald) → `data.strengthLog {from, to, sessions}` (all_weeks bærer ikke done for tidligere uger), bygger `data.body` fra `history` + ugens sessions (ikke-blokerende). `kpis.weight`/`kpis.fat` = `_body.weight_kpi/fat_kpi` (fallback neutral uden body) — **ikke** `color_for` mod 68 kg/16 %. Den gamle RHR/HRV-cut-alarm (linje ~467-498) er erstattet af `cut_warning(body)` → én regel-advarsel `type 'cut'` (action null via `merge_warnings`). `weightToGoal`/`bodyFatToGoal` fjernes fra data.json (`pop`). |
| `scripts/modules/coach_context.py` | `body.cut` får, når `data.body` findes: `phase, status, expectedKg (fra body), corridorKg, actualRatePerWeek4w, ffmKg, ffmChange28d, ffmTargetKg, fatAvg14, fatExpected, checkLevel, checkText`. Uden `data.body` uændret (eksisterende tests grønne). |
| `scripts/modules/test_body.py` (ny, 27 tests) | Faser/datoer/rate/ISO-uge, lineær forventning, korridor + 2-mandags-regel (foran/bagud/plan-med-note), hold-korridor, ingen vejninger, faktisk rate, fedt-status, FFM, styrke-uge/-log/-pr.-uge, cut-tjek (inaktiv pre/hold, på plan, ingen data, rate warn/act, ffm act vinder, styrke warn, restitution warn kun når vægt falder, plateau info), KPI'er (pre/cut-farver), advarsel, `build_body` mod rigtig plan.json (pre + cut), coach-kontekst med/uden body. |

## B. index.html

- **Krop**: hero `.gp-hero` "CUT UGE 4 AF 19 · UGE 42" + statusord (Foran/På plan/Bagud i status-farve; "Ingen vejninger" uden 7d-snit), linje "7d-snit 70,9 · forventet 71,6 · −0,7 kg", "−0,22 kg/uge planlagt · −0,33 faktisk (4 uger)", note (2-mandags-noten). Pre: "Cut starter uge 39 · 21/9", "nu: vedligehold · 7d-snit …", plan-linje. Hold: "VEDLIGEHOLD · UGE n" + korridor.
- **Glidepath-graf** (`glidepathGraph`/`buildGlidepathSVG`, registreret i `SPARKLINE_DATA` med `builder:'glide'`, så `redrawSparklines` tegner den 1:1 i den målte bredde): korridor-bånd ±0,5 (muted 13 %), stiplet forventet linje 21/9→31/1 med sluttal, lodret "i dag", 7d-snit i `--gold` (stiplet ved huller ≥ 3 dage), punkt + tal (under punktet når snittet ligger under planen), x-akse i kalenderuger (uge 39 … uge 4), y dynamisk. Pre: 90 dages 7d-snit med datolabels + startpunkt 21/9 markeret ("start 72,2 · uge 39").
- **Tre tiles** (`.kpi3`): FEDT 14D (forventet), FEDTFRI (Δ28d, mål), STYRKE n/2 (uge n).
- **Cut-tjek-kort** (`.cutcheck` med level-farvet venstrekant ok/info/warn/act): én handlingstekst, `<details>` med de fem signaler (prik + værdi eller "ingen data"). Pre: "Aktiveres uge 39 (21/9)".
- Tendenser: vægt-sparkline fjernet (grafen ovenfor); fedt-sparkline viser **14d-snit** (`sparkline(..., {avgDays:14, subLabel:'14d-snit ±0,5'})` — `movingAvg7`/`trendFromAvg`/`buildSparklineSVG` har fået et `winDays`-parameter, default 7 → HRV/RHR/søvn uændrede). "x kg til mål"-takeaway fjernet; "siden d/m"-linjen bevaret. `STATIC.weight/bodyFat` + `progressMetric` slettet. AF-kort, protein-kort, energi uændrede.
- **I dag** `kpiStripV2`: VÆGT = `body.glidepath.avg7`, FEDT = `body.fat.avg14`; farve på tallet fra status (plan/foran `--ok`, bagud orange, pre muted); sub "forventet 71,6" / "cut starter uge 39" / "vedligehold 68,0 ±1,0". Ingen nye tiles.
- **Kalenderuge**: statuslinjen "Søndag 6. sep · Uge 36 · RACE" (`isoWeekOf`, ISO 8601, beregnet client-side); sidebar "UGE 36 · program-uge 1/51". Plan-fanens blokstribe og uge-overskrift viser `isoWeekOf(w.start)` ("UGE 39 · 21.–27. SEP") — kun label. Mere-fanen uændret.

## C. Verifikation

- `screens-v3/shoot_body.py` (Playwright, Chromium fra `/opt/pw-browsers`, `python3 -m http.server 8765`): rigtig data.json + `data.body` beregnet offline via body.py mod plan.json. Scenarie **pre** = 6/9; scenarie **cut** = 2026-10-12 (browser-uret sat med `page.clock.set_fixed_time`), syntetisk vægt **0,7 kg under** glidepath fra 21/9 — spec'en sagde 0,4, men det ligger inden for korridoren ±0,5 og giver 'plan'; 0,7 giver 'foran'. Fedt flad 21,0, styrke 2/uge, RHR/HRV flade. iPhone 390×844 @2x, lys + mørk. **Ingen JS-fejl.**
  Screenshots: `screens-v3/krop-{pre,cut}-fold-{light,dark}.png`, `krop-{pre,cut}-fuld-*.png` (signaler foldet ud), `idag-{pre,cut}-fold-*.png`, `plan-cut-fold-*.png`; `body-pre.json`/`body-cut.json` = det beregnede `data.body`.
- Rettet efter første runde: punkt-labelen i grafen lå oven i den stiplede plan-linje → under punktet når snittet er under planen; fedt-/styrke-tile-subs kortet ned så de ikke ombrydes til tre linjer; FEDT-tile på I dag ligeså; "-0 %" i restitutions-signalet → `_signed` (dansk komma, ægte minus, aldrig "−0").
- Store tal og grafen læsbare i mørk tilstand (status-ord i lyse grøn/rav-toner på det mørke hero; korridor-bånd i `--muted` 13 %, 7d-snit i `--gold`).

## Kendte risici / ikke gjort

- I det syntetiske cut-scenarie slår FFM-signalet ud (act), fordi vægten falder 0,7 kg mens fedtprocenten holdes flad — det er matematisk korrekt og viser handlingsteksten, men også en påmindelse om at FFM fra bioimpedans er støjende; tærsklen −0,5 kg/28 d kan vise sig for følsom i praksis (`FFM_ACT_KG` i body.py).
- `data.body`, `strengthLog` og de nye `kpis.weight/fat` findes først efter næste `update_kpi.py`-kørsel. Indtil da viser Krop-heroen "Cut-status kommer fra data.json" og I dag-tiles falder tilbage til de gamle KPI-farver.
- Styrke-loggen kræver ét ekstra Intervals-kald (28 dage aktiviteter); fejler det, bruges den cachede `strengthLog` fra data.json (evt. ingen → 'ingen data' i signalet).
- Kalenderugen i statuslinjen beregnes af browserens ur, ikke af data.json — hvis data er fra i går, kan "Uge 37 · RECOVERY" vise ny uge med gammel blok. Blokken kommer stadig fra data.
- `coach_context.body.weightToGoal` (coachens afledte tal til validatoren) er bevaret — kun data.json-felterne er fjernet.
- Screenshots og `shoot_body.py` er ikke committet (utracked i `screens-v3/`, som de tidligere blokke).
